# Design & Planning

## What is my goal?
1. Make a website that has two tabs. One if Fun Fact(FF). Another is Cross Check(CC).
2. FF requirements
    - FF is simple. It has a date. It has a rectangle. Inside a rectangle, it shows a daily fun fact.
    - Here I want to learn hands-on knowledge in Typescript. I also want to use async/await call to AI model. (start with Claude?)
    - Optional: Use local LLM if you're going offline?
3. CC requirements
    - I first want to build a chatapp using websocket
    - You ask a system design question
    - I want to give 2 different AI model answers
    - Optional: Use Local LLM if you're going offline